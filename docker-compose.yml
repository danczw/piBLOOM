services:
  pibloom_api:
    build:
      context: ./piBLOOM_api
      args:
        - N_WORKERS=2 # number of uvicorn workers for unicorn api server
    ports:
      - 5000:5000

  pibloom_web:
    build: ./piBLOOM_web
    ports:
      - 8080:80